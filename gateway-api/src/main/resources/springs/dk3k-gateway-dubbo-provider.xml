<?xml version="1.0" encoding="UTF-8"?>
<beans xmlns="http://www.springframework.org/schema/beans" xmlns:xsi="http://www.w3.org/2001/XMLSchema-instance" 
		xmlns:dubbo="http://code.alibabatech.com/schema/dubbo" xsi:schemaLocation="http://www.springframework.org/schema/beans
       http://www.springframework.org/schema/beans/spring-beans.xsd
       http://code.alibabatech.com/schema/dubbo
       http://code.alibabatech.com/schema/dubbo/dubbo.xsd">

	<!-- 提供方应用信息，用于计算依赖关系 -->
	<!-- <dubbo:application name="gateway-api" owner="zhou" /> -->

	<!-- 使用zookeeper注册中心暴露服务地址 -->
	<!-- <dubbo:registry protocol="zookeeper" address="${dubbo.zk.url}" timeout="${dubbo.zk.timeout}" /> -->

	<!-- 用dubbo协议在20888端口暴露服务,默认端口20880,port=-1, 当默认端口被占用，在20880端口自增1 -->
	<!-- <dubbo:protocol name="dubbo" port="${dubbo.port}" />  -->

	<!-- <dubbo:monitor protocol="registry" /> -->

	<!-- 提供的服务-->
	<!-- <dubbo:service ref="testCleanSpi" interface="com.dk3k.TestCleanSpi" timeout="20000" /> -->


</beans>